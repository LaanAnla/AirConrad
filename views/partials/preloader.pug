.preloader
  .preloader__brands
    img.preloader__brands__image(src=preloader.data.image.url alt=preloader.data.image.alt)
    .preloader__number 
      .preloader__number__text 0%
  .preloader__video 
    video.video__preloader(src="" crossorigin="anonymous" preload autoplay muted controls)
    //- video.video__preloader(src=preloader.data.video1.url preload autoplay muted controls)
    //- iframe.video__preloader(src="https://www.youtube.com/embed/aHY1p_af5_U" title="YouTube video player" frameborder="0" allow="autoplay" allowfullscreen)
    .preloader__video__close 
      p.skip__video skip introduction 